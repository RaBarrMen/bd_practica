import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF1976D2);
  static const Color primaryDark = Color(0xFF1565C0);
  static const Color primaryLight = Color(0xFF42A5F5);

  // Colores secundarios
  static const Color secondary = Color(0xFFFFC107);
  static const Color secondaryDark = Color(0xFFFFB300);

  // Colores de estatus
  static const Color statusPending = Color(0xFFFFA500); // Naranja
  static const Color statusCompleted = Color(0xFF4CAF50); // Verde
  static const Color statusCancelled = Color(0xFFF44336); // Rojo

  // Colores neutros
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF757575);
  static const Color greyLight = Color(0xFFEEEEEE);
  static const Color greyDark = Color(0xFF424242);

  // Colores de puntos en calendario
  static const Color dotPending = Color(0xFF4CAF50); // Verde
  static const Color dotCompleted = Color(0xFFFFFFFF); // Blanco
  static const Color dotCancelled = Color(0xFFF44336); // Rojo

  // Colores para UI
  static const Color backgroundColor = Color(0xFFF5F5F5);
  static const Color cardColor = Color(0xFFFFFFFF);
  static const Color dividerColor = Color(0xFFBDBDBD);

  // Colores de texto
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);

  // Sombra
  static const Color shadowColor = Color(0x1F000000);
}